---
interface Props {
  type: 'article' | 'note' | 'bookmark' | 'like' | 'photo' | 'reply' | 'writing' | 'award' | 'album';
  url: string;
  date: Date;
  title?: string;
  summary?: string;
  quote?: string;
  linkTo?: string;
  photo?: string;
  alt?: string;
  venue?: string;
  category?: string;
  truncated?: boolean;
}

import { formatDate } from '../lib/date';

const { type, url, date, title, summary, quote, linkTo, photo, alt, venue, category, truncated } = Astro.props;
---

<div class={`feed-item feed-item--${type} h-entry`} data-category={category ?? type}>
  <a href={url} class="feed-time"><time datetime={date.toISOString()}>{formatDate(date)}</time></a>
  <svg class="feed-type" width="20" height="20" aria-hidden="true"><use href={`/icons.svg#icon-${type}`}/></svg>
  <div class="feed-content">
    {type === 'article' && (<>
      <p><a class="p-name u-url" href={url}>{title}</a></p>
      {quote && <p>{quote} <a href={url} aria-label={`Read ${title}`}><svg width="16" height="16" aria-hidden="true"><use href="/icons.svg#icon-readmore"/></svg></a></p>}
    </>)}
    {type === 'note' && (
      <p class:list={['feed-note', { truncated }]}>{summary}</p>
    )}
    {type === 'bookmark' && (<>
      <p><a href={linkTo}>{title ?? linkTo}</a></p>
      {summary && <p class:list={{ truncated }}>{summary}</p>}
    </>)}
    {type === 'like' && (
      <p><a href={linkTo}>{linkTo}</a></p>
    )}
    {type === 'photo' && (<>
      {photo && <a href={url}><img class="u-photo" src={`${photo}?class=photo400`} srcset={`${photo}?class=photo200 200w, ${photo}?class=photo400 400w`} sizes="(min-width: 640px) 40rem, 100vw" alt={alt ?? title ?? ''} loading="lazy" decoding="async" /></a>}
      {title && <p>{title}</p>}
    </>)}
    {type === 'reply' && (<>
      <p>Replied to <a href={linkTo}>{linkTo}</a></p>
      {summary && <p class:list={{ truncated }}>{summary}</p>}
    </>)}
    {type === 'writing' && (<>
      <p>{linkTo ? <a href={linkTo}>{title}</a> : title}</p>
      {(venue || category) && <p>{[venue, category ? category.charAt(0).toUpperCase() + category.slice(1) : ''].filter(Boolean).join(' — ')}</p>}
      {summary && <p class:list={{ truncated }}>{summary}</p>}
    </>)}
    {type === 'award' && (<>
      <p>{title}</p>
      {venue && <p>{venue}</p>}
    </>)}
    {type === 'album' && (<>
      {photo && <a href={url}><img class="u-photo" src={`${photo}?class=photo400`} srcset={`${photo}?class=photo200 200w, ${photo}?class=photo400 400w`} sizes="(min-width: 640px) 40rem, 100vw" alt={title ?? ''} loading="lazy" decoding="async" /></a>}
      <p><a href={url}>{title}</a></p>
      {summary && <p>{summary}</p>}
    </>)}
  </div>
</div>
